version: 1
frontend:
  phases:
    build:
      image: public.ecr.aws/docker/library/node:20
      commands:
        - npm ci
        - npm run build
  artifacts:
    baseDirectory: /dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*